{% extends 'base.html.twig' %}

{% block title %}New Calendar{% endblock %}

{% block body %}
    <div class="container mt-15">
        <h1>Prenez votre rendez-vous</h1>
        {{ include('calendar/_form.html.twig') }}
        <a href="{{ path('app_calendar_index') }}" class="btn btn-success btn-lg btn-block">Voir vos reservation</a>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Réserver votre créneau dans la {{ nom }}{% endblock %}

{% block body %}
    <div class="container mt-15">
        <h1>Réserver votre créneau dans la {{ nom }}</h1>
        {{ include('calendar/_form.html.twig') }}
        <br>
        <a href="{{ path('app_calendar_index') }}" class="btn btn-success btn-lg btn-block">Retourner à vos réservations</a>
    </div>
{% endblock %}
